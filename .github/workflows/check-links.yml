# Check all external URLs in .qmd source files for broken links.
# Runs on every push to main and weekly on Mondays.
# Can also be triggered manually via the Actions tab.
#
# Author: Miriam Marling <miriam@BonQuery.ca>
# Date: 2026

name: Check links

on:
  push:
    branches: [main]
    paths:
      - "**/*.qmd"
      - "**/*.md"
  schedule:
    - cron: '19 9 * * 1'    # Every Monday at 09:19 UTC
  workflow_dispatch:

jobs:
  check-links:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check links
        uses: lycheeverse/lychee-action@v2
        with:
          # Check all .qmd and .md files; skip mailto: links and localhost
          args: >
            --verbose
            --no-progress
            --exclude "^mailto:"
            --exclude "^https?://localhost"
            --exclude "^https?://127\."
            --exclude "^https://www\.linkedin\.com"
            --exclude "^https?://bonquery\.ca"
            --exclude "^https://www\.cdc\.gov/niosh/bulletin/2012/cold-stress\.html$"
            --exclude "^https://www\.mayoclinic\.org/diseases-conditions/frostbite/symptoms-causes/syc-20372656$"
            --exclude "\.csv$"
            "**/*.qmd"
            "**/*.md"
          # Fail the workflow when broken links are found so GitHub sends
          # an email notification. This does not block pushes or the data
          # refresh workflow — both run independently of this check.
          fail: true
